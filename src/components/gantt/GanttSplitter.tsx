import styled from 'styled-components';

const GanttSplitter = styled.div`
    flex: 0 0 0.5em;
    background: #e5e5e8;
    --splitter-button-size: 1em;
    z-index: 5;
    position: relative;
`;

export default GanttSplitter;
